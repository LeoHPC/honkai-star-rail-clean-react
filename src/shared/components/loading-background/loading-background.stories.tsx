import { Meta, StoryObj } from '@storybook/react'

import { LoadingBackground } from './loading-background'

export default {
  title: 'Components/Loading Background',
  component: LoadingBackground
} as Meta

export const Default: StoryObj = {}
